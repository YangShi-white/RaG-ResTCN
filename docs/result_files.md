# Result CSV Files

`results/paper_tables/` contains compact CSV tables used to support the paper text and supplementary material.

`results/figure_source_data/` contains source CSV files used for the manuscript figures. Generated image files are intentionally excluded from this clean review package.

The full pipeline can regenerate corresponding output artifacts under `outputs/` after the raw datasets are placed locally.
