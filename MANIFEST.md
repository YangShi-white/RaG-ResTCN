# Clean Review Package Manifest

Included:

- Source code under `src/fermnftp/`.
- Runnable experiment scripts under `scripts/`.
- Public configuration files under `configs/`.
- Data availability and reproducibility documentation.
- Example SLURM scripts for GPU and CPU execution.
- Compact paper-result CSV files under `results/`.

Excluded:

- Raw datasets.
- Local logs, scheduler output files, checkpoints, and returned HPC folders.
- LaTeX build artifacts.
- Generated figure images.
- Project-planning notes and intermediate private reports.
